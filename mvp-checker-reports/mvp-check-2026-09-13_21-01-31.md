# MVP Readiness Report — Herbs Masters Duel

**Generated**: 2026-09-13 21:01:31

Domain inferred from the repo: a browser-only, client-side game (Astro static site, no backend/server), per `context/foundation/prd.md`. Judged accordingly — no login is treated as a candidate deliberate design choice, not automatically penalized. Deployment (Cloudflare Workers) and visual design are out of scope for this analysis per the prompt's instructions.

## Checklist

| # | Criterion | Status |
|---|---|---|
| 1 | CRUD actions | ✅ |
| 2 | Business logic | ❌ |
| 3 | Tests addressing a defined risk | ✅ |
| 4 | Authentication tied to a user | ✅ |
| 5 | Documentation | ✅ |

**Project Status: 4/5 → 80%**

## Details

**1. CRUD actions — ✅**
`src/lib/storage/index.ts` still implements all four operations against a herb-card deck, persisted via `src/lib/storage/adapter.ts`'s `localStorage`-backed envelope: `createCard()`, `listCards()`, `updateCard(id, patch)`, `deleteCard(id)`. Since the last report, `src/lib/storage/seed.ts`'s `seedDefaultCards()` was added as a new *consumer* of this same `createCard` — it doesn't add a new CRUD path, just populates the existing one from bundled seed data.

**2. Business logic — ❌**
Still no function implements real domain logic — no memory-matching rules, shuffle logic, turn-passing, scoring, or tie detection (PRD's `## Business Logic` section, FR-006–FR-012; roadmap slice `S-02`). `seedDefaultCards()` (new since the last report) is data population, not game logic — it doesn't compute or decide anything specific to the product's value.

**3. Tests addressing a defined risk — ✅**
`context/foundation/test-plan.md` still names its two risks, and `adapter.test.ts` still maps to both (6/6 passing). Since the last report, `src/lib/storage/seed.test.ts` added 2 more tests (seeding an empty deck; leaving a non-empty deck untouched) — good coverage, but not itself required for this criterion since it doesn't target a risk named in `test-plan.md`.

**4. Authentication tied to a user — ✅**
Unchanged: `getPlayerHistory(name)`/`appendGameResult(name, result)` still scope game-result history by the player's typed name, reasonable for this local, no-login, two-player design.

**5. Documentation — ✅**
Unchanged and still substantial: `context/foundation/{prd.md, roadmap.md, infrastructure.md, tech-stack.md, shape-notes.md, test-plan.md}`.

## Priority Improvements

1. **Business logic** — Still the single highest-leverage gap, unchanged from the last report. Implement the game engine per roadmap slice `S-02` (`two-player-memory-match`): deck shuffling with the FR-005 pair-count rule (10–24, capped at deck size), flip/match/mismatch state transitions (FR-006–FR-008, FR-010), turn-passing, and tie detection (FR-012). `S-01` (herb-card-authoring UI) is the natural next step first, since `S-02` depends on it, and both are now unblocked by real seed data (`herb-seed-cards`) to develop and test against without waiting on the image-lookup API decision.

---

*No change in status from the previous report (`mvp-check-2026-09-13_19-19-37.md`, also 4/5, 80%) — the `herb-seed-cards` work (seed data + `seedDefaultCards()`) landed since then, but it extends an already-✅ criterion (CRUD) rather than closing the one remaining gap (business logic).*
