# MVP Readiness Report — Herbs Masters Duel

**Generated**: 2026-09-14 04:23:14

Domain inferred from the repo: a browser-only, client-side game (Astro + React island, no backend/server), per `context/foundation/prd.md`. Judged accordingly — no login is treated as a candidate deliberate design choice, not automatically penalized. Deployment and visual design are out of scope per the prompt's instructions.

## Checklist

| # | Criterion | Status |
|---|---|---|
| 1 | CRUD actions | ✅ |
| 2 | Business logic | ✅ |
| 3 | Tests addressing a defined risk | ✅ |
| 4 | Authentication tied to a user | ✅ |
| 5 | Documentation | ✅ |

**Project Status: 5/5 → 100%**

## Details

**1. CRUD actions — ✅**
Unchanged: `src/components/CardForm.tsx` creates (`createCard`) and edits (`updateCard`) cards; `src/components/CardManager.tsx` reads (`listCards`) and deletes (`deleteCard`, gated by an inline confirm). Reachable from the live app via `Menu` → "Create Cards".

**2. Business logic — ✅ (strengthened since the last report)**
Previously this criterion was cleared only by `src/lib/wikipedia.ts`'s image-lookup integration — genuine logic, but *supporting* logic for card creation, not the product's core mechanic. That gap is now closed: `src/lib/game.ts` implements the actual memory-matching engine — `selectTile`'s match/mismatch/turn-passing state machine, `resolveMismatch`, `getWinner`'s win/tie detection, `resolvePlayerNames`'s same-name auto-suffixing, and `pickRandomSubset`'s deck sampling — all pure, all unit-tested (27 tests in `src/lib/game.test.ts`). This is the PRD's own stated source of the product's value ("the memory mechanic itself... is what produces retention"), not incidental logic.

**3. Tests addressing a defined risk — ✅**
Unchanged: `context/foundation/test-plan.md` names two risks, both covered by `adapter.test.ts`. `game.test.ts`'s 27 tests are thorough but — like `wikipedia.test.ts` before it — aren't mapped to a named risk in `test-plan.md`; good coverage, but not itself what satisfies this criterion.

**4. Authentication tied to a user — ✅ (strengthened since the last report)**
Previously `getPlayerHistory`/`appendGameResult` existed and were tested but "not wired into any UI." That's now done: `src/components/GameEndScreen.tsx` calls both at the end of every real game, scoping score history by player name (with same-name collisions resolved to distinct identities via `resolvePlayerNames`, e.g. `"Alex (1)"`/`"Alex (2)"`) — a reasonable design for this local, no-login, two-player game.

**5. Documentation — ✅**
Unchanged and still substantial, now also including `context/changes/two-player-memory-match/` (plan, plan-brief, and a full implementation review with 3 triaged findings, all fixed).

## Priority Improvements

All five criteria remain met — no priority improvements required to clear the minimal technical bar. With `S-02` (`two-player-memory-match`, the roadmap's north star) now implemented and reviewed, the full MVP loop the PRD describes as its single Primary Success Criterion is playable end-to-end: author cards → set up two players → play a full memory-matching game → see a winner/tie and score. The project has moved from "clears the letter of the criteria" to "the criteria are backed by the actual core gameplay loop."

---

*No change in overall checklist status from the previous report (`mvp-check-2026-09-14_02-49-09.md`, 5/5, 100%) — but the evidence behind criteria 2 and 4 has substantively strengthened, now that `S-02` (the core memory-matching game) is implemented and reviewed, rather than resting on supporting/unwired code.*
