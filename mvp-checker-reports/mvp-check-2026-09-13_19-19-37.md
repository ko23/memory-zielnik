# MVP Readiness Report — Herbs Masters Duel

**Generated**: 2026-09-13 19:19:37

Domain inferred from the repo: a browser-only, client-side game (Astro static site, no backend/server), per `context/foundation/prd.md`. Judged accordingly — no login is treated as a candidate deliberate design choice, not automatically penalized. Deployment (Cloudflare Workers) and visual design are out of scope for this analysis per the prompt's instructions.

## Checklist

| # | Criterion | Status |
|---|---|---|
| 1 | CRUD actions | ✅ |
| 2 | Business logic | ❌ |
| 3 | Tests addressing a defined risk | ✅ |
| 4 | Authentication tied to a user | ✅ |
| 5 | Documentation | ✅ |

**Project Status: 4/5 → 80%**

## Details

**1. CRUD actions — ✅**
`src/lib/storage/index.ts` implements all four operations against a herb-card deck, persisted via `src/lib/storage/adapter.ts`'s `localStorage`-backed envelope (not in-memory-only):
- **Create**: `createCard()` (index.ts:50-67)
- **Read**: `listCards()` (index.ts:46-48)
- **Update**: `updateCard(id, patch)` (index.ts:69-86) — operates on an already-persisted card by id, then re-persists; not a pre-save transient edit.
- **Delete**: `deleteCard(id)` (index.ts:88-97)

These are service/repository-layer methods (no UI wired to them yet — that's roadmap slice `S-01`), which the criterion explicitly accepts as valid evidence.

**2. Business logic — ❌**
No function implements real domain logic yet — no memory-matching rules, shuffle logic, turn-passing, scoring, or tie detection (PRD's `## Business Logic` section, FR-006–FR-012; roadmap slice `S-02`). The persistence module's own logic (versioned envelopes, fail-soft error handling, trimmed/case-insensitive name matching) is infrastructure, not domain logic reflecting the product's unique value.

**3. Tests addressing a defined risk — ✅**
`context/foundation/test-plan.md` names two concrete risks. `src/lib/storage/adapter.test.ts` maps directly to both, 6/6 passing (`npm run test`):
- Risk 1 ("corrupted/invalid data must not crash the app") → tests asserting `readEnvelope` returns the caller's default on invalid JSON and on a mismatched `schemaVersion`, rather than throwing.
- Risk 2 ("a storage write failure must not crash card creation") → a test mocking `localStorage.setItem` to throw and asserting `writeEnvelope` returns `{ ok: false }` rather than throwing.

**4. Authentication tied to a user — ✅**
No login system exists, which remains a reasonable design for a two-player local-device game with no server (PRD Access Control, reaffirmed in Non-Goals on 2026-09-12). What changed since the last report: the PRD's "remembered per player name" requirement is now actually implemented, not just described — `getPlayerHistory(name)` and `appendGameResult(name, result)` (index.ts:99-113) scope game-result history by the player's typed name (trimmed, case-insensitive), which is the lightweight identity/scoping model this domain calls for.

**5. Documentation — ✅**
`context/foundation/` now includes `prd.md` (13 FRs plus the newly-added FR-014 for card deletion), `roadmap.md` (milestone `M-1` with foundation `F-01` marked `in-progress`, slices `S-01`/`S-02`), `infrastructure.md`, `tech-stack.md`, `shape-notes.md`, and the new `test-plan.md`. Substantial, non-placeholder content throughout.

## Priority Improvements

1. **Business logic** — This is now the single highest-leverage gap. Implement the game engine per roadmap slice `S-02` (`two-player-memory-match`): deck shuffling with the FR-005 pair-count rule (10–24, capped at deck size), flip/match/mismatch state transitions (FR-006–FR-008, FR-010), turn-passing, and tie detection (FR-012). Run `/10x-plan two-player-memory-match` once `S-01` (card authoring UI) exists, since `S-02` depends on it for cards to play with.

Note: the project has moved from 20% to 80% since the last check by landing exactly the two things flagged as highest-leverage gaps before (CRUD persistence + a first risk-based test), plus incidentally resolving the auth-scoping gap as a side effect of the same work. Only business logic (the actual game engine) remains to clear the full technical bar.
