# MVP Readiness Report — Herbs Masters Duel

**Generated**: 2026-09-13 00:59:09

Domain inferred from the repo: a browser-only, client-side game (Astro static site, no backend/server), per `context/foundation/prd.md`. Judged accordingly — no auth/backend is treated as a candidate deliberate design choice, not automatically penalized. Deployment (Cloudflare Workers, live at `herbs-masters-duel.ko23.workers.dev`) is out of scope for this analysis per the prompt's instructions.

## Checklist

| # | Criterion | Status |
|---|---|---|
| 1 | CRUD actions | ❌ |
| 2 | Business logic | ❌ |
| 3 | Tests addressing a defined risk | ❌ |
| 4 | Authentication tied to a user | ❌ |
| 5 | Documentation | ✅ |

**Project Status: 1/5 → 20%**

## Details

**1. CRUD actions — ❌**
No application code exists yet beyond the stock Astro "basics" template: `src/pages/index.astro` still renders only the default `Welcome` component; `src/components/`, `src/layouts/`, `src/assets/` are all untouched scaffold files. There is no code anywhere that creates, lists, edits, or deletes a herb card (or any other persisted item) — the PRD describes this (FR-001–FR-003, FR-013 for editing) and `context/foundation/roadmap.md` now sequences it as slice `S-01`, but none of it is implemented yet.

**2. Business logic — ❌**
No function implements the memory-matching rule, shuffle logic, turn-passing, scoring, or tie detection described in the PRD's `## Business Logic` section and FR-006–FR-012 (roadmap slice `S-02`). There is no game-engine code in the repository at all yet.

**3. Tests addressing a defined risk — ❌**
No test plan found (checked `context/foundation/` — `infrastructure.md`, `prd.md`, `README.md`, `roadmap.md`, `shape-notes.md`, `tech-stack.md` are present; no `test-plan.md`). No test files exist anywhere in the repo (`*.test.*` / `*.spec.*` search returned nothing), and no test framework is installed (`package.json`'s only `devDependencies` entry is `wrangler`, added for deployment — not testing).

**4. Authentication tied to a user — ❌**
The PRD's Access Control section (local profile, no login, flat model, player names instead of accounts) is a reasonable design for a two-player local device game, and a login system would genuinely be over-engineering here — this was explicitly reconsidered and reaffirmed in the PRD's Non-Goals (most recently on 2026-09-12). However, this criterion needs evidence that *something* identifies/scopes a user in the actual system, and no code implementing even that lightweight player-name scoping exists yet — there's no application logic at all to evaluate.

**5. Documentation — ✅**
`context/foundation/prd.md` is a substantial, non-placeholder PRD: vision, persona, success criteria, 13 functional requirements (FR-001–FR-013) with Socratic counter-argument resolutions, a one-sentence business rule, NFRs, access control, and non-goals. Since the last report, the foundation has grown further: `context/foundation/roadmap.md` sequences the PRD into a milestone (`M-1: First Playable Duel`) with a dependency-ordered foundation (`F-01` local persistence) and two vertical slices (`S-01` card authoring, `S-02` gameplay), and `context/foundation/infrastructure.md` records the researched, bias-checked deployment decision. `shape-notes.md` and `tech-stack.md` back all of this with the discovery process and stack rationale. Note: the root `README.md` is still Astro's generic starter boilerplate, not project-specific — worth a quick rewrite for polish, though it doesn't block this criterion since the foundation docs carry the substance.

## Priority Improvements

1. **CRUD** — Build the "Create Cards" flow per roadmap slice `S-01` (FR-001–FR-003, FR-013): a form to add a herb card (name + looked-up image), a list view of existing cards, edit/replace, and delete. Persist to `localStorage` or `IndexedDB` via the roadmap's `F-01` local-persistence foundation (not just in-memory component state) so cards survive a refresh, per the PRD's own guardrail and NFRs. Run `/10x-plan local-persistence-scaffold` then `/10x-plan herb-card-authoring` to start.
2. **Business logic** — Implement the game engine as its own module (e.g. `src/lib/game.ts`) under roadmap slice `S-02`: deck shuffling with the FR-005 pair-count rule (10–24, capped at deck size), flip/match/mismatch state transitions (FR-006–FR-008, FR-010), turn-passing, and tie detection (FR-012). This is the differentiator that keeps the project from being empty CRUD — prioritize it alongside #1.
3. **Tests** — Write `context/foundation/test-plan.md` naming a concrete risk (e.g., "a mismatch that fails to flip tiles back leaks information and breaks the game's core memory guarantee," or "the pair-count validator could allow starting a game with more pairs than cards exist, crashing the board"). Install Vitest (`npm install -D vitest`, the natural fit for an Astro/TS project) and add at least one test exercising that exact risk against the game-engine module from #2.
4. **Authentication** — No login needed given the domain (and the PRD formally documents this decision), but once the game screens exist, make sure the two player names entered at setup (FR-004, FR-011) are what actually scope and label turn state and scores in code — that's what would satisfy this criterion for a local-only game like this one.

Meeting all five clears the minimal technical bar; it doesn't by itself guarantee certification, but right now the project is still 100% documentation with zero implemented functionality — CRUD and business logic remain the two highest-leverage gaps, since tests and the auth-equivalent scoping both depend on that code existing.

---

*No change from the previous report (`mvp-check-2026-09-12_14-26-22.md`) — no application code has changed since; only foundation docs (`prd.md` deadline update, new `roadmap.md`, new `infrastructure.md`) and the deployment pipeline (out of scope for this check) were added in the interim.*
